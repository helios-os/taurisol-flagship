{
  "name": "taurisol-monorepo",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "build:dev": "cd apps/web && npm run build",
    "typecheck": "cd apps/web && npx tsc -p tsconfig.json --noEmit"
  }
}
