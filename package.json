{
  "name": "taurisol-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "build:dev": "npm --prefix apps/web run build",
    "typecheck": "npx tsc -p apps/web/tsconfig.json --noEmit"
  }
}
