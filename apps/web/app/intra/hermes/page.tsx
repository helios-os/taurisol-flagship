import type { Metadata } from "next";
import HermesPage from "./HermesPage";

export const metadata: Metadata = {
  title: "I build communities — Taurisol Living Lab Intra",
};

export default function Page() {
  return <HermesPage />;
}
